image_processing:
  api_key: ${ZHIPUAI_API_KEY}
  cache_enabled: true
  cache_ttl: 3600
  context_window: 3
  enabled: true
  max_concurrent: 5
  retry_count: 3
  storage_path: storage/images
  timeout: 30
  vision_model: glm-4v-plus
